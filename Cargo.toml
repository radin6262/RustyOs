[package]
name = "rusty"
version = "0.1.0"
edition = "2024"

[workspace]
resolver = "3"
members = ["kernel"]

[build-dependencies]
bootloader = { version = "0.11.17", default-features = false, features = ["uefi"] }
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
